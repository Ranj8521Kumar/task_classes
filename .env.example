# Backend key for /api/solve route.
# If missing, server returns a sample fallback explanation
OPENAI_API_KEY=
# Optional: add your OpenAI API key for live AI answers.
# If missing, the app uses built-in sample fallback answers.
VITE_OPENAI_API_KEY=
